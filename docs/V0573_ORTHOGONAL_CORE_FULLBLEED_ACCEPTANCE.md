# V0.57.3-alpha 正交核心營業區滿版與分區 人工驗收清單（ARCH-0573）

- 版本：`V0.57.3-alpha｜正交滿版營業區原型版`｜Build `0573a`
- 決策：[DEC-020](./decisions.md#dec-020正交核心營業區滿版與分區metadataaccepted)｜結果：[V0573 結果](./V0573_ORTHOGONAL_CORE_FULLBLEED_RESULT.md)
- 用途：本清單供產品負責人在**真實裝置**逐項勾選；未勾選前不得宣稱實機通過，也不得開始 `ART-0574` 家具重作。

## 如何開啟

本機（ES Modules 需 HTTP，不可 `file://`）：

```powershell
cd "C:\Users\rober\Desktop\貓咪咖啡廳"
py -m http.server 8765
```

- 首屏滿版（Demo 構圖）：`http://<手機看得到的IP>:8765/?projection=ortho&demoLayout=1`
- 真實存檔（非 Demo）：`?projection=ortho`
- 疊 Art Debug：`?projection=ortho&demoLayout=1&artDebug=1`
- iso 回退（rollback）：`/` 或 `?projection=iso`

## A. 手機直立滿版（核心營業區）

- [ ] iPhone 直立首次進入，**核心營業區縱向填滿**畫面（上下不再有大片留白）。
- [ ] 首屏不需先縮放或大量左右拖曳，即可看懂 入口→櫃檯→座位→貓咪 的分區與主走道。
- [ ] 左右僅少量裁切（外圈欄）；入口、櫃檯、主座位、主走道、貓咪區**皆完整未被裁**。
- [ ] 390 / 393 / 430 三種寬度都合理滿版（可換機型或瀏覽器 device toolbar 測）。

## B. 兩格入口門與分區

- [ ] 上牆**右側兩格**看得到入口門；門**右邊仍是牆**（門不貼房間右邊）。
- [ ] 門在牆的下半、完整可見；門上方仍是牆。
- [ ] 入口下方一格為入口地墊；由門往下是清楚的**主走道**（約 2 格寬）。
- [ ] 櫃檯帶在上方、員工工作區在櫃檯**後方**、顧客點餐/排隊在櫃檯**前方**，方向可辨識。

## C. Camera 邊界與縮放

- [ ] 兩指縮小可看見**整間房間**（含外圈與整面牆），縮到底不出現空白背景。
- [ ] 放大後平移，四個方向都**停在房間邊緣**，不會拖出房間露出大片底色。
- [ ] 外圈被裁的欄，可用拖曳補看，且拖曳有邊界。

## D. 未破壞既有

- [ ] 家具可選取、拖曳、旋轉、收納、出售（用 `?projection=ortho` 非 Demo 模式測）。
- [ ] 重整後存檔正常；家具位置沒有跑掉、金幣沒有變動。
- [ ] `/` 或 `?projection=iso` 仍是原本 2:1 等角（rollback 可用）。
- [ ] Demo 模式重整後不會把 Demo 家具寫進存檔（切回 `?projection=ortho` 看到的是自己的存檔）。

## E. 裝置矩陣（逐項勾選）

| 裝置 / 瀏覽器 | 首屏核心滿版 | 兩格門/分區可辨識 | 縮放/平移邊界 | 既有功能 |
|---|---|---|---|---|
| iPhone Safari 直立 | ☐ | ☐ | ☐ | ☐ |
| Android Chrome 直立 | ☐ | ☐ | ☐ | ☐ |
| 桌面 Chrome | ☐ | ☐ | ☐ | ☐ |
| 桌面 Edge | ☐ | ☐ | ☐ | ☐ |

## 判定

- [ ] **通過**：進入 `ART-0574` 核心家具正交重作（10～12 件；仍保留 furniture ID 與存檔相容）。
- [ ] **不通過**：記錄實機問題（附機型、截圖、網址參數），交回 Claude Code 調整 framing / 分區，不改存檔契約。

> 參考截圖（自動化 real-browser，非實機）：`docs/evidence/v0573/`（首屏 390/393/430、pan 四邊界、minzoom/zoomin、art-debug、桌面、before/after）。實機驗收仍以上述清單為準。
